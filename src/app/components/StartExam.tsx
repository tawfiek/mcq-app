import { Component,  ReactNode } from "react";

export class StartExam extends Component {
    render(): ReactNode {
        return (
            <div>This is the first component to render</div>
        )
    }
}